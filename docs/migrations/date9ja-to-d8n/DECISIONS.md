# Date9ja Parity Decision Queue

This is the single queue for unresolved decisions. `CAPABILITY-PARITY.md` is authoritative for retained user capabilities and counts. Durable engineering decisions belong in `docs/adr/`; this queue records the decision needed and its effect.

## Product owner

| Decision | Why it matters | Capability | Blocker | Recommended options | Status |
|---|---|---|---|---|---|
| Retain tribe | Sensitive field may affect display, matching, and migration | Profiles | Yes | Private catalogue; matching-only; archive-only | Awaiting Uchechi. Engineering machinery exists (2026-09-05, ADR 0030): canonical `sensitive_identity` + `storage: :pending` `FieldCatalog` definition, deterministically rejected on write, enabled by no brand. No product exposure decision made. |
| Retain ethnicity | Sensitive field may affect display, matching, and migration | Profiles | Yes | Private catalogue; matching-only; archive-only | Awaiting Uchechi. Same machinery as "Retain tribe" above (ADR 0030). No product exposure decision made. |
| Retain denomination | Sensitive field may affect display, matching, and migration | Profiles | Yes | Private catalogue; private-only; archive-only | Awaiting Uchechi. Deferred pending a hierarchy/representation decision (flat vocabulary vs. nested under a selected religion) — no `FieldCatalog`/`CapabilityCatalog` definition exists yet (ADR 0030). |
| Retain preferred tribes | Matching preference affects recommendations | Matching/Profiles | Yes | Typed preference; matching-only; archive-only | Awaiting Uchechi. Deferred as a preference/matching concept, not an ordinary profile scalar — no definition exists yet (ADR 0030). `preferred_religion` carries the same deferral, not yet a separate row. |
| Retain genotype | Sensitive health-adjacent data requires explicit purpose and consent | Profiles/Trust | Yes | Do not migrate; private consented use; archive-only | Awaiting Uchechi. Deferred pending product + security/modelling decision — no `FieldCatalog` definition exists yet (ADR 0030). |
| Exact verification gates | Determines publication, interaction, and badge behavior | Verification/Trust | Yes | Preserve current gates; staged step-up; approved strengthening | Awaiting Uchechi |
| Approved photo publication | Prevents surprise hiding or re-review loops | Media/Moderation | Yes | Preserve approved state; review exceptions; full re-review | **RESOLVED — preserve legacy behaviour: pending photos visible, rejected excluded (`:immediate` policy). Applies to profile video too (ADR 0023).** |
| Profile video retained | Introduction video is parity | Media | Yes | Retain as shared Media capability | **RESOLVED — retain; ADR 0023; implemented this batch pending review** |
| Legacy `Photo.is_primary` → D8N ordering | D8N `ProfilePhoto` has `position` but no `is_primary` | Media | Yes (pass 2) | Drop primary; primary→position 0; keep source order | **RESOLVED (2026-09-03) — primary→destination `position 0`, remaining photos keep relative source order behind it; zero primaries → deterministic source order (position 0 = effective primary); multiple primaries for one retained profile → FAIL CLOSED / quarantine for product review, never arbitrary. Pass 1 only measures the three cases. ADR 0027.** |
| Legacy `Photo.moderation_status` → D8N | Authoritative enum `pending:0/approved:1/rejected:2` | Media/Moderation | Yes (pass 2) | — | **RESOLVED (2026-09-03) — `pending`→`pending_review` (immediate-publication visibility), `approved`→`approved`/`visible`, `rejected`→`rejected`/`hidden`. Unknown values fail closed. No change to the existing publication-policy decision. ADR 0027.** |
| Legacy owners with >6 photos vs D8N max 6 | No destructive normalization | Media | Yes (pass 2) | Truncate; quarantine; raise the cap | **RESOLVED (2026-09-03) — no silent drop/truncate. Pass 1 measures `owners_over_six` + `max_photos_per_owner` and preserves all source evidence; the cap/quarantine call is a pass-2 product decision. ADR 0027.** |
| Generalize `Migration::MediaTransfer` across media kinds (PD-1) | Pass 2 planning found the verified transfer spine is image-coupled; a second consumer (video, later chat video) needs media-specific verification but the same locking/identity/recovery | Migration | Yes (pass 2, architecture) | (A) small injected media-kind strategy on the existing spine; (B) separate `Migration::VideoMediaTransfer` framework | **RESOLVED (2026-09-03) — A. `Migration::MediaTransfer::MediaKind` strategy parameterizes ONLY genuinely media-specific concerns (accepted content types, extension, byte ceiling, magic-byte detector, structural/container validation, remote re-verification body). Locking, canonical identity structure, `AdoptOrUpload` state machine, deterministic recovery, `ReferenceMap` semantics and source/destination identity rules are NOT parameterized. `MediaKind::Image` reproduces today's behaviour byte-for-byte; the Profile Photo call site and its Codex-verified semantics are unchanged. Recorded as ADR 0029; ADR 0028 remains the historical accepted image architecture. Not implemented.** |
| Legacy profile video exceeding D8N's server-enforced duration limit | No destructive normalization; Date9ja client-trusted duration, D8N enforces it | Media | OPEN (pass 2, evidence-gated) | (A) grandfather as-is; (B) migrate + trim/re-encode; (C) quarantine for review | **DEFERRED to pass 2 — evidence-gated (2026-09-03). Video pass 1 found `profile_videos.duration_seconds` NULL for all 35 observed legacy rows (`duration_missing` 35 / 35): no source row is *known* to exceed the D8N limit, but actual duration is *unproven* — Date9ja did not persist it, and pass 1 does not read media bytes. Refined contract (ADR 0029): Pass 2 Phase A derives authoritative duration (ffprobe via `Media::VideoProcessor.probe`, corroborated by `Media::VideoContainerValidator` `mvhd`) BEFORE any destination adoption, outside DB locks. Unreadable → FAIL CLOSED `quarantined` / `duration_unreadable`. Over limit → FAIL CLOSED `quarantined` / `duration_over_limit` — no destination blob, no `ProfileVideo`, no `ReferenceMap` binding, no processing job. `Media::ProcessProfileVideoJob` re-checks during processing as defence in depth. If any migrated video exceeds the limit, STOP and pick A/B/C. NOT "moot" — deferred until real duration is known from the L2 rehearsal / actual media.** |
| Photo byte-transfer source access (Pass 2) | Migration must read legacy R2 media without broad permanent prod credentials in app config | Media/Migration/Security | Yes (pass 2) | (A) scoped read-only legacy R2 token, run-env only; (B) STS/temp creds; (C) pre-exported controlled media bundle; (D) other | **RESOLVED (2026-09-03) — A: a dedicated read-only, bucket-scoped Cloudflare R2 credential in the migration run's environment only (never `config/`, credentials, DB, logs); source/destination creds separate; reader is `HEAD`/`GET` only, HTTPS-only, no-redirect, host-allowlisted, fail-closed. C (pre-exported bundle) is the fallback. Token custody/rotation is operator logistics. `MEDIA-TRANSFER.md` §5, ADR 0028.** |
| Publication when a photo is quarantined (Pass 2) | If an owner's only/primary photo fails transfer/validation, may the profile still publish? | Media/Profiles product | Yes (pass 2) | Block publication; publish without it; publish with next photo as primary | **RESOLVED (2026-09-03) — a bad NON-PRIMARY photo does not automatically block an otherwise-valid profile. The profile is flagged for explicit review when (a) the failed/quarantined photo is the owner's only usable photo, or (b) a failed source primary prevents deterministic primary semantics. Migration never silently selects a new source-semantic primary; a reviewed exception may later approve publication. `MEDIA-TRANSFER.md` §17.** |
| Cutover gate strictness for media (Pass 2) | Whether media failures must be zero before cutover | Migration/ops | Yes (cutover) | Zero required; small manually-reviewed tail acceptable | **RESOLVED (2026-09-03) — target is ZERO UNEXPLAINED media failures. Reviewed/explicitly-approved exceptions may exist. Final reconciliation distinguishes `unexplained_failure` vs `reviewed_exception`; cutover requires `unexplained_failure == 0`. Nonzero `quarantined`/`processing_failed` is not silently accepted. `MEDIA-TRANSFER.md` §15, §18.** |
| `source_removed` photo at cutover delta | A photo deleted in production between bulk pre-copy and final snapshot | Migration | Yes (cutover) | Auto-flag for review (no auto-delete of the D8N `ProfilePhoto`); auto-remove; ignore | **RESOLVED (2026-09-03) — auto-flag/reconcile; never auto-delete destination data inside the importer; final authoritative snapshot governs; explicit operator/product cleanup decision if removal is warranted. `MEDIA-TRANSFER.md` §18.** |
| Suspended-owner media (Pass 2) | Whether suspended owners' retained photos migrate | Media/Migration | Yes (pass 2) | Migrate structurally; drop; photo-level hide | **RESOLVED (2026-09-03) — retain photos structurally; suspension is not deletion; existing profile/membership suspension suppresses public delivery; no photo-specific hiding added unless shared delivery semantics require it. `MEDIA-TRANSFER.md` §14.** |
| Photo `service_name` census (Pass 2) | Pass 1 did not record `active_storage_blobs.service_name` | Migration | Yes (pass 2) | — | **RESOLVED (2026-09-03) — census run against `date9ja_snapshot_sanitized`: `cloudflare` = 279, no other services. Single-service corpus. Pass 2 re-asserts against the final production snapshot. `RECONCILIATION.md`.** |
| Verification retained | All shipped/reachable Date9ja verification stays in the bar | Verification/Trust | Yes | Retain all checks/states/badges | **RESOLVED — retain; ADR 0024 defines the architecture; evidence-retention/provider decisions still open (Mixed)** |
| Trust / reputation retained | Existing Trust XP state + history is parity | Trust | Yes | Retain; ledger→derived→presentation | **RESOLVED — retain; ADR 0025; point values migrate verbatim, no new scoring; user-visible presentation still open** |
| Founding/premium access to retain | Prevents loss or accidental grant of paid access | PAY/Entitlements | Yes | Migrate active entitlement; time-bound bridge; exception queue | **RESOLVED — no migrated user loses an existing founding/premium/permanent entitlement; ADR 0026; no new plans/pricing** |
| Signed-out password reset for an unverified email | Date9ja's Devise `:recoverable` let an unconfirmed email reset its password; D8N's `RecoveryRequester` requires a verified identifier (ADR 0012), and a verified phone does not substitute (the password credential is bound to the email identifier). 79/288 census accounts are unconfirmed — they keep their password (login works) and gain self-serve reset after verifying the email. | Identity/Verification | No — password login unaffected; Phase-5 parity-acceptance question | Keep ADR 0012 (verify-then-reset); soften for the migrated unconfirmed cohort; time-bound bridge | **Surfaced 2026-09-04 (Wave A Step 3 closeout). Default = keep ADR 0012. Not an implementation blocker. Decide at Phase-5 parity acceptance. `AUTH-TRANSITION.md`.** |
| Historical profile-view visibility | Determines whether and how history is exposed | Engagement/Profile Views | Yes if retained | Full history; recent window; archive-only | Awaiting Uchechi |
| User-visible trust score/history | A raw score may misrepresent safety status | Trust | Yes if visible | Derived badge; score plus explanation; history private | Awaiting Uchechi |
| Material support-chat behavior | Determines whether support appears as a special inbox or ordinary conversation | Messaging/Support | Only if behavior changes | Shared support surface; conversation specialization; approved bridge | Awaiting Uchechi only if behavior changes |
| Active-surface retirement | Only explicit product retirement may remove a reachable feature | All | Yes | Retain all; formally retire named surface | Awaiting Uchechi |
| Final treatment of self-deleted / banned accounts (auth/recovery path) | Determines whether migrated members can sign back in / recover | Identity | Yes (Step 3) | Migrate as recoverable; skip; skip + cutover-delta reconcile | **RESOLVED (2026-09-04) — by source analysis. Date9ja `DELETE /api/v1/account` is password-confirmed soft deletion; `User#active_for_authentication?` returns false for a deleted account and there is NO consumer undelete/reactivation route (30-day grace → `AccountHardDeleteJob` anonymisation). "Account recovery" in Date9ja = password reset only (Devise `:recoverable`), preserved by D8N's recovery flow. The importer skipping `deleted_at` (`source_soft_deleted`) and `banned_at` (`source_banned`) rows is parity-correct — those members could not sign in to Date9ja either. `Identity::AccountReactivation` covers D8N-native self-deactivation only (additive). Residual edge: an account inside its 30-day grace window at snapshot time is dropped with no D8N presence — handled by the established media cutover-delta rule (auto-flag/reconcile, never auto-create/auto-delete destination data, final authoritative snapshot governs). `AUTH-TRANSITION.md`.** |

## Engineering / architecture

| Decision | Why it matters | Required before |
|---|---|---|
| D8N AI assistant contract and domain boundary | Defines runtime, provider isolation, tools, safety, and brand configuration | AI implementation `SPECIFIED` |
| AI provider data egress, retention, redaction, logging, credentials, consent, fallback, and cost controls | Aunty Phobie may process sensitive user conversations | AI implementation `SPECIFIED` |
| Verification evidence model and retention | Evidence is sensitive and may have provider dependencies | Verification implementation `SPECIFIED` — **architecture specified in ADR 0024 (Proposed); evidence-retention/provider/portability decisions in "Mixed" below still gate implementation** |
| Profile video / media boundary | Retained parity; must be a shared Media capability, not a Date9ja fork | Profile-video implementation — **ADR 0023 (Proposed); model + pipeline + Date9ja contract implemented this batch pending review** |
| Entitlement preservation model | Existing founding/premium rights must survive cutover without new commercial behaviour | Entitlement implementation `SPECIFIED` — **architecture specified in ADR 0026 (Proposed)** |
| Engagement/profile-view domain boundary | Avoids duplicate history and notification semantics | Engagement implementation `SPECIFIED` |
| Community shared-capability boundary | Community is in parity but must not become a Date9ja fork | Community implementation `SPECIFIED` |
| Dating Hub primitive decomposition and ownership | Prevents copying the legacy monolith | Dating Hub implementation `SPECIFIED` |
| Trust ledger and derived reputation architecture | Separates auditable events from user-visible status | Trust implementation `SPECIFIED` — **architecture specified in ADR 0025 (Proposed); "User-visible trust score/history" product row still gates presentation** |
| Genotype privacy/model architecture | Determines whether data is modelled, encrypted, or excluded | Any genotype modelling/import |
| Canonical scalar profile field catalogue and brand-scoped write/serialization enforcement | Prevents a Date9ja profile subsystem and per-brand field-registry drift; blocks writable Date9ja profiles until resolved | Writable Date9ja profiles — **RESOLVED (2026-09-05) — implemented as `Profiles::FieldCatalog`; ADR 0030 Accepted. Date9ja proven as a full explicit consumer with zero new production code (`STATUS.md`). Does not resolve the sensitive-field product decisions above.** |
| Legacy operational mapping to D8N HQ | Ensures safe administration before legacy retirement | Legacy admin retirement |
| External legacy reference map (source↔destination binding, immutability, tenant safety) | Deterministic idempotent spine for every importer slice | Wave A slice 2 — **ADR 0022 accepted by independent review; product-owner acknowledgment recorded by normal ADR workflow** |
| Recovery-required credential (unusable legacy bcrypt digest + operable channel) | An account whose legacy digest is missing/malformed must not be silently lost, and must not become an unreachable account (`AUTHENTICATION.md`) | Wave A Step 3 closeout — **RESOLVED (2026-09-04; Codex Finding 1 fix). `Date9ja::Import::IdentityImport`: unusable digest + `FieldMapping.operable_recovery_channel?` (a **verified email** — the identifier the password `Credential` is bound to and the one `Identity::RecoveryRequester`/`PasswordReset` resolve the credential through) → migrate with an active `kind: password` `Credential` and NO `CredentialPasswordHash`; first access = signed-out recovery → `PasswordEngine.set!`. A verified **phone alone is NOT** an operable channel (shared recovery has no cross-identifier fallback and inventing one would be Date9ja-specific auth infrastructure) → fail closed. Unusable digest + no operable channel → `credential_hash_unusable`, no account. Rerun completeness = `IdentityImport#credential_completeness` (`:complete` needs a supported bcrypt hash — `BCRYPT_RE` + `BCrypt::Password.new`; missing → `:incomplete`; unsupported → `:corrupt_credential`/`credential_hash_corrupt`; a member replacement counts complete, never clobbered). Census (2026-09-02): 0 affected — cutover robustness. No new ADR — extends ADR 0012; flagged as an ADR candidate if the pattern recurs. `AUTH-TRANSITION.md`.** |
| bcrypt credential + migrated-account session/recovery compatibility | Authentication-sensitive. **The sanitized rehearsal snapshot does NOT suffice** — it carries one inert digest, no real hashes. Needs the separate operator seed-account procedure. **Procedure defined + implemented 2026-09-02.** Discovery run (operator, pristine restore): **one bucket only — `$2a$` cost 12, 288 accounts, 0 malformed, no Devise pepper**. Proof implemented as `scripts/date9ja/bcrypt_proof.rb` (+ synthetic-value tests): copies the legacy digest verbatim (no `set!`), asserts `Identity::PasswordEngine.matches?` true / wrong-password false / `PasswordLogin` session / stored hash byte-identical, inside a rolled-back transaction, printing only `$2a$ 12 PASS|FAIL`. **VERIFIED (2026-09-02) — the operator ran it with a real operator-owned `$2a$12$` Date9ja account and reported `$2a$ 12 PASS`.** The legacy digest authenticates through the real D8N password path with no rehash and the stored hash stays byte-identical. Migrated-account session/recovery design follows `AUTHENTICATION.md` (fresh D8N session on first login; one-time secure recovery only if a hash ever fails). **Wave A Step 3 CLOSEOUT — IMPLEMENTED / SELF_VERIFIED (2026-09-04):** the migrated account authenticates, gets a brand-scoped session, cannot cross brands, recovers (session-revoking), and reactivates through the shared services with no Date9ja-specific auth infrastructure; unusable-digest accounts with a verified channel migrate recovery-required rather than being dropped. `Date9ja::Import::AuthTransitionCheck` + L1 synthetic rehearsal; `date9ja:verify_auth_transition` operator L2 not yet run. `AUTH-TRANSITION.md`. | Wave A slice 3 credential step **READY** — proof passed; **Step 3 closeout SELF_VERIFIED, awaiting feature-boundary Codex review** |
| Import execution model — how an importer reads the sanitized snapshot | Sets the shape of the snapshot reader (a shared `Migration` primitive) and how CI exercises it. Operator owns the environment; engineering owns the reader. | **RESOLVED (2026-09-02) — RESTORED SCRATCH POSTGRESQL DATABASE.** The importer reads from a controlled restore of a Date9ja backup (`Date9ja backup → controlled restore → Date9ja source adapter/snapshot reader → shared D8N migration primitives → deterministic `ReferenceMap` → D8N ID/Profile/downstream → reconciliation`). Not a live production connection. NDJSON/CSV is permitted only as later diagnostics/export tooling, never as the canonical migration path. Rationale: repeatable rehearsals; no runtime dependency on live Date9ja; safer rollback/inspection; deterministic source state; identical migration shape against the sanitized rehearsal artifact and a future cutover restore; migration code needs no broad production DB access. Date9ja source-schema knowledge stays inside `scripts/date9ja/*` + the Date9ja adapter; `domains/migration/*` and D8N shared capabilities stay product-agnostic. **Exercised 2026-09-03:** `Date9ja::Import::IdentityImport` rehearsal against `date9ja_snapshot_sanitized` — 280/288 imported, 8 documented skips, idempotent on rerun, source-census reconciliation balanced (`STATUS.md`, `RECONCILIATION.md`). |

| Migration media preflight (source blob vs. attachment vs. destination identity) | Photo/video/message media migration needs a home for source-media graph + transfer state that is neither `ProfilePhoto` nor `ReferenceMap` | Wave A profile-photo pass 1 — **ADR 0027 Accepted; `Migration::MediaObjectRef` / `MediaAttachmentRef` implemented + rehearsal VERIFIED 2026-09-03** |
| Migration media byte transfer (source-storage adapter, non-transactional recovery) | Pass 2 must stream legacy R2 bytes into D8N private storage and create real `ProfilePhoto`s idempotently, without weakening shared media guarantees | Wave A profile-photo pass 2 — **ADR 0028 ACCEPTED (2026-09-03); IMPLEMENTATION VERIFIED + L2 SYNTHETIC-CORPUS REHEARSAL VERIFIED (Codex independent review 2026-09-03: FINAL VERDICT ACCEPT — L2 review loop closed; still NOT PARITY_ACCEPTED / production-ready / cutover-ready; L3 NOT YET READY, separate operational/security gate). History: IMPLEMENTED / SELF_VERIFIED (2026-09-03); implementation-review round 1 (Codex BLOCK) fixes applied — authoritative deterministic-chain `already_transferred` (no prefix inference), Phase-B owner re-resolution, NO remote I/O under any DB lock (`LockGuard` + A/B/C `AdoptOrUpload`), async never reports premature success (`transferred` only after ready + validated deterministic display), bounded destination verification, exact canonical-content-type equality, strict `SourceReader` key grammar, retry-exhaustion → terminal. Implementation-review round 2 (Codex BLOCK — 2 defects) fixes applied: (a) ONE authoritative display-validation contract — shared `Media::DisplayDerivative.valid?` (D8N media domain; exact deterministic key + service + content type + `Blob.checksum` integrity + JPEG decode over a bounded remote read); `Media::ProcessProfilePhotoJob` runs it OUTSIDE every DB lock and no longer trusts any metadata-only check as a success/ready-no-op/purge path (CLAIM → `:verify_ready` → `reconcile_ready` confirm / rebuild-from-raw / fail-closed-terminal); exact key re-derived after raw purge from persisted `ProfilePhoto#metadata` (existing jsonb, not a new table); `Migration::MediaTransfer.valid_accepted_display?` delegates to it. (b) Phase-B `finalize_binding` rejects an existing Photo→ProfilePhoto binding that is the same user but wrong profile/brand → `binding_conflict`/`mapping_drift`, never `already_transferred`, never a silent reparent. Code + L1 automated tests green, RuboCop/Zeitwerk/Brakeman clean, NOT independently re-reviewed, NOT VERIFIED/PARITY_ACCEPTED, `_media_v2` NOT built, L2/L3 NOT run, no bytes moved. FINAL Codex acceptance-check correction: `canonical_content_type` (verified detected media type) is now a DECLARED canonical-identity field and is in the canonical string before the UUIDv5 — because it drives `original.<ext>` — so the migration storage key `migrations/media/v3/date9ja/profile_photo_original/<UUIDv5>/original.<ext>` is a TOTAL deterministic function of the declared identity (`version v3 | source_system | source_blob_id | source_attachment_id | destination_purpose | destination_brand | canonical_content_type`). Verified source content-type drift → `source_changed`, never a silent re-key. Every prior Codex blocker (Rev 1 → FINAL check) closed. Pass 2 implementation READY; L1/L2 ready after implementation; L3 real R2 blocked only on operator logistics. Rev 4 baseline (3 Codex FINAL-review blockers, all closed): (1) migration storage key is a dedicated `CanonicalKey.final_key` (NOT `Media::ObjectKey.profile_photo_original`) — no user id, no profile public id, no mutable `Brand#slug`; destination remap → `mapping_drift`/`binding_conflict`, no re-key; (2) short attachment lock — Phase A (prepare/verify, NO lock) → Phase B (short finalization txn) → Phase C (after-commit enqueue); no R2/hash/upload/libvips under the `MediaAttachmentRef` lock; tiny blob-row coordination txn distinct from the finalization lock; (3) abandoned processing claim — `ProfilePhoto` gains nullable `processing_started_at` + `processing_claim_token` (processing-lifecycle hardening, NOT a migration table); claim/reclaim/finalize/failure gate on `processing_state == processing AND claim_token == my_token` (token REQUIRED — bare timestamp loses the ABA race); sweeper reclaims stale `processing`. Shared seam FINAL: `Profiles::PhotoUpload.build_photo!` extraction. Retained: no destination state on `MediaObjectRef`, no new migration table (inference-based recovery); Active Storage cases 1–6, no case-4 adoption, per-reuse real remote-byte verification; four-concept orphan model; distinct `_media_v2` rehearsal artifact; explicit `SourceReader` security contract. `ProfilePhoto` enum/state model, `Media::PhotoPolicy`, `MediaObjectRef`, `MediaAttachmentRef`, `ReferenceMap` unchanged.** |

## Mixed

| Decision | Product question | Engineering question | Status |
|---|---|---|---|
| Entitlements | What legacy/founding/premium access is retained? | How PAY/Entitlements represents and enforces it | Product pending; engineering follows |
| Trust presentation | What do users see? | How ledger/events produce that status | Product pending; architecture required |
| Verification provider use | Which user-visible checks are retained? | Which providers, evidence boundaries, and retention rules are safe? | Product/security pending |

Support-chat ownership is no longer a pure product decision: engineering owns the reusable architecture investigation and returns only material user-visible choices. Founder/admin operations are settled as D8N HQ scope, not Date9ja consumer parity; remaining HQ operational gaps are tracked in the matrix operational register.
