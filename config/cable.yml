development:
  adapter: async

test:
  adapter: test

production:
  # No D8N channel is currently exposed. This built-in adapter avoids an unused
  # Redis or Solid Cable dependency until realtime messaging is justified.
  adapter: postgresql
  channel_prefix: d8n_production
